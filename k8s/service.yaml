apiVersion: v1
kind: Service
metadata:
  name: health-monitor-svc
  labels:
    app: health-monitor
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: health-monitor